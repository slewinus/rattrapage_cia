project            = "cia"
environment        = "prod"
location           = "northeurope"
admin_username     = "azureuser"
ssh_public_key_path = "~/.ssh/id_rsa.pub"

# Restrict SSH to your IP (recommended)
allowed_ssh_cidrs = ["203.0.113.10/32"]

# Expose Gitea SSH to the same IP range (or adjust)
gitea_ssh_port = 2222
allowed_gitea_ssh_cidrs = ["203.0.113.10/32"]

# Optional: auto-clone repo on the VM
# git_repo_url   = "https://github.com/your-org/rattrapage_CIA.git"
# git_repo_branch = "main"

# Optional extra Azure tags
# tags = {
#   owner = "you"
#   cost_center = "demo"
# }

