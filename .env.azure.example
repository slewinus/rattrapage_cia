# Azure-specific hostnames for Traefik routing

# Replace <IP_DASH> with your VM IP using dashes, e.g. 20-50-60-70
# Or set to your own DNS names if you manage DNS (recommended)

APP_HOST=app.<IP_DASH>.sslip.io
API_HOST=api.<IP_DASH>.sslip.io
GRAFANA_HOST=grafana.<IP_DASH>.sslip.io
PORTAINER_HOST=portainer.<IP_DASH>.sslip.io
GITEA_HOST=gitea.<IP_DASH>.sslip.io
TRAEFIK_HOST=traefik.<IP_DASH>.sslip.io

# If you want Gitea SSH exposed on another port, set it here.
# Ensure the NSG allows that port (var.gitea_ssh_port) and the ops compose uses it.
# GITEA_SSH_PORT=2222

# Traefik ports (Azure should use 80/443)
TRAEFIK_HTTP_PORT=80
TRAEFIK_HTTPS_PORT=443

# Let's Encrypt contact email
ACME_EMAIL=admin@example.com
